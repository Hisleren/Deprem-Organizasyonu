name: deprem_uygulamasi
description: "Deprem Bilgi ve Yardım Uygulaması"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_map: ^4.0.0
  latlong2: ^0.8.1
  flutter_map_marker_popup: ^4.0.0
  firebase_core: ^2.30.0
  cloud_firestore: ^4.17.5
  firebase_auth: ^4.18.0
  html: ^0.15.6
  provider: ^6.0.5
  intl: ^0.18.1
  geolocator: ^11.0.0
  google_fonts: ^4.0.4
  url_launcher: ^6.1.11
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/drone_logo.png
